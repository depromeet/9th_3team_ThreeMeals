import * as React from 'react'
import { storiesOf } from '@storybook/react'
import ContactUsPage from '../../components/pages/ContactUsPage'

storiesOf('page/Contact', module).add('ContactUsPage', () => {
  return <ContactUsPage />
})
