import * as React from 'react'
import { storiesOf } from '@storybook/react'
import ContentPage from '../../components/pages/ContentPage'

storiesOf('page/Content', module).add('ContentPage', () => {
  return <ContentPage />
})
